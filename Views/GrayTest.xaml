<UserControl
    x:Class="LLC_MOD_Toolbox.Views.GrayTest"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:LLC_MOD_Toolbox.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:sys="clr-namespace:System;assembly=mscorlib"
    xmlns:viewmodels="clr-namespace:LLC_MOD_Toolbox.ViewModels"
    d:Background="DarkGray"
    d:DataContext="{d:DesignInstance Type=viewmodels:SettingsViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    FontFamily="Assets/Fonts/#Source Han Sans CN"
    FontSize="20"
    FontWeight="Medium"
    Foreground="#FFFFEED4"
    mc:Ignorable="d">
    <UserControl.Resources>
        <ControlTemplate x:Key="TextBoxTemplatePlaceHolder" TargetType="TextBox">
            <Border
                x:Name="border"
                Padding="5,0"
                Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}"
                SnapsToDevicePixels="True">
                <Grid>
                    <ScrollViewer
                        x:Name="PART_ContentHost"
                        Focusable="false"
                        HorizontalScrollBarVisibility="Hidden"
                        VerticalScrollBarVisibility="Hidden" />
                    <TextBlock
                        x:Name="PlaceHolder"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Background="Transparent"
                        Opacity="0"
                        Text="{TemplateBinding Tag}" />
                </Grid>
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="Text" Value="">
                    <Setter TargetName="PlaceHolder" Property="Opacity" Value="0.7" />
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>
    </UserControl.Resources>
    <StackPanel>

        <TextBlock TextWrapping="Wrap">
            请您在文本框当中输入提供的测试密钥，并点击确认。成功进入灰度测试模式之后，请使用自动安装页面进行安装。此时，自动安装的文件将会是密钥相对应的灰度测试文件。
        </TextBlock>

        <Image Source="/Assets/Images/NormalLine.png" />
        <TextBlock FontWeight="Bold" Text="秘钥" />
        <TextBox
            FontSize="16"
            FontWeight="Medium"
            Tag="{DynamicResource PlaceHolder}"
            Template="{StaticResource TextBoxTemplatePlaceHolder}"
            Text="{Binding TestCode}"
            TextWrapping="Wrap">
            <TextBox.Resources>
                <sys:String x:Key="PlaceHolder">请输入测试密钥</sys:String>
            </TextBox.Resources>
        </TextBox>
        <Button
            Content="校验"
            FontSize="16"
            FontWeight="Heavy" />
    </StackPanel>
</UserControl>
