<Window x:Class="LLC_MOD_Toolbox.Services.Update.ToolboxUpdateDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="工具箱更新"
        Width="460"
        SizeToContent="Height"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="#FFF8F1"
        FontFamily="/Fonts/subset/#Source Han Sans CN">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="发现工具箱新版本"
                   FontSize="20"
                   FontWeight="Bold"
                   Foreground="#3A2618"
                   Margin="0,0,0,12"/>

        <TextBlock Grid.Row="1"
                   x:Name="SummaryText"
                   TextWrapping="Wrap"
                   FontSize="14"
                   LineHeight="22"
                   Foreground="#4B3526"
                   Margin="0,0,0,14"/>

        <Border Grid.Row="2"
                BorderBrush="#D4B28A"
                BorderThickness="1"
                Background="#FFFFFA"
                CornerRadius="4"
                Padding="12"
                Margin="0,0,0,14">
            <StackPanel>
                <TextBlock x:Name="VersionText"
                           FontSize="13"
                           Foreground="#3A2618"
                           Margin="0,0,0,6"/>
                <TextBlock x:Name="SourceText"
                           FontSize="13"
                           Foreground="#6C4A32"/>
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="3"
                    Margin="0,0,0,18">
            <ProgressBar x:Name="DownloadProgress"
                         Minimum="0"
                         Maximum="100"
                         Height="12"
                         Visibility="Collapsed"/>
            <TextBlock x:Name="StatusText"
                       TextWrapping="Wrap"
                       FontSize="13"
                       Foreground="#6C4A32"
                       Margin="0,8,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button x:Name="UpdateButton"
                    Content="立即更新"
                    Width="92"
                    Height="30"
                    IsDefault="True"
                    Click="UpdateButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
